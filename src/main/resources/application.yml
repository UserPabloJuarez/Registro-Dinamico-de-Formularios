spring:
  application:
    name: formulario-service
  data:
    mongodb:
      uri: mongodb://mongo:27017/formulariosdb
      auto-index-creation: true

server:
  port: 8080

logging:
  level:
    com.example.formulario_service: INFO

app:
  security:
    api-key: "formulario-api-key-2025"
    enabled: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
